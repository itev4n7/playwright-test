{
  "name": "playwright-test",
  "version": "1.0.0",
  "description": "Test framework using Playwright with Page Object pattern",
  "main": "index.js",
  "scripts": {
    "test": "playwright test --config=playwright/configs/playwright.config.js",
    "inspector": "PWDEBUG=1 npm run test",
    "test:tag-fast": "playwright test --config=playwright/configs/playwright.config.js --grep @fast",
    "test:tag-slow": "playwright test --config=playwright/configs/playwright.config.js --grep @slow"
  },
  "keywords": [],
  "author": "Oleksii Kvasov",
  "license": "ISC",
  "dependencies": {
    "playwright": "^1.21.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.21.1",
    "allure-playwright": "^2.0.0-beta.15"
  }
}
